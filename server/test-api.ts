import 'dotenv/config'; import express from 'express'; import { updateMyProfile } from './src/controllers/volunteerController'; const app = express(); app.use(express.json()); app.put('/test', (req, res, next) => { (req as any).user = { id: 3 }; next(); }, updateMyProfile); const server = app.listen(5001, async () => { try { const res = await fetch('http://localhost:5001/test', { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ fullName: 'API Test', phoneNo: '999999999' }) }); const data = await res.json(); console.log('API Response:', data); } catch (e) { console.error(e); } finally { server.close(); process.exit(0); } });